import { StyleSheet } from 'react-native';

const favoritesST = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#f0f0f7'
    },
    teacherList:{
        marginTop: -40,
    }
})

export default favoritesST